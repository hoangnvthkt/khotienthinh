# Vioo Work R1A Task 9

Continue the approved detail/capability-actions checkpoint in the existing isolated
worktree, main agent only, Cloud only. Keep the feature and notification gates off.

1. Add guarded subject read projections for display names/scope metadata, eligible
   transfer/co-assignee candidates and exact notification comment anchors. Reuse
   existing canonical eligibility and command rules; add rollback persona tests.
2. Add typed lifecycle/collaboration readers and commands, immutable retry attempts,
   explicit conflict recovery and bounded lazy thread/history state. Test contracts.
3. Build responsive detail: contextual task rail, description, result, checklist,
   assignment acknowledgement/SLA, participants, pin/mute, capability-driven actions
   and reason/confirmation forms. Preserve list filters and scroll on return.
4. Build comment/reply/edit/authorized mentions and exact comment deep links;
   lazy filtered history with safe human-readable event evidence.
5. Build private attachments: lazy thumbnails, fresh signed display/download URLs,
   input/discussion/result/evidence uploads, same-reservation retries and deletion.
6. Verify isolated desktop/tablet/mobile browser personas, conflict and lost-response
   cases, permission denial and list return; lint/full tests/build/query audit;
   commit candidate, isolated migration dry-run/apply and Cloud postflight.
7. Record normal rollout evidence and commit. No handoff or pilot activation.
