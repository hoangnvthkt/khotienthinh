# Project V2 → Procurement V2 technical validation

Validation date: 24 September 2026. Git branch: `feature/refactor-du-an-t9-1`. Implemented through Task 12 at `0e3f3f9c8adfb25818eec30b91a07f2feb6fd3ae`. This record covers **technical evidence only**. It does not activate a production cohort, change the DA29 G9 manifest, or sign off business UAT.

## Worktree and scope protection

- Before Task 13, `git status --short` and `git diff --name-only` showed dirty `components/procurement/*`, `components/project/material/*`, daily-log/auth files, `supabase/baseline/current.json`, `types.ts`, and `docs/audits/erp-end-to-end-2026-09-19/README.md`, plus untracked `docs/designs/` and `docs/references/`. These belong to other workstreams or user material and are excluded from Project V2 commits.
- The existing `docs/designs/erp-completion-2026-09-19/HANDOFF.md` is itself untracked and contains the G9 pilot handoff. It must not be added wholesale to a Project V2 commit. The Project V2 runbook and this evidence file can be staged by exact path.
- The shared migration baseline marker does not allowlist the Project V2 migration files. `npm run check:supabase-migrations` is red for this reason and for an unrelated in-progress daily-log migration; the marker was not modified for this workstream.

## Local verification

| Check | Result | Scope |
| --- | --- | --- |
| Focused V2 Vitest | PASS: 28 files, 130 tests | Project V2, material-plan procurement, Procurement V2, purchase draft and PO migration tests. |
| Full Vitest | PASS: 520 files, 2 skipped; 2448 tests, 2 skipped | Entire current worktree, including unrelated local edits. |
| Vite build | PASS | Application bundle. |
| Supabase query audit | PASS: 0 findings, 0 errors | Checked against the existing query inventory. |
| `git diff --check` | PASS | Current unstaged diff. |
| Repository `npm run lint` | FAIL outside Project V2: 185 TypeScript errors under untracked `docs/references/vioo-project-v2-codex-handoff/prototype-source` | Scoped TypeScript check reported no Task 12 file errors. The unrelated prototype is included by the repository-wide tsconfig and lacks its own Next/Radix dependencies. |
| Migration baseline gate | FAIL: V2 files and unrelated daily-log file unallowlisted in dirty shared marker | No baseline marker change was staged. |

## UI and command evidence

- Project V2 component walkthrough: 10 Playwright checks passed for source picker, month/construction/material editors, calculation basis, collaboration pagination, protected references, dirty-form comment guard, 390/768/1440 px, and 200% zoom. The 390 px collaboration screenshot was visually inspected; no horizontal overflow.
- Procurement V2 component walkthrough: 5 Playwright checks passed for dossier and PO editor at 390/768/1440 px, keyboard focus, 200% zoom and dark mode. These fixture checks do not establish a named-persona business journey on an actual preview route.
- Task 11 Cloud preview: rollback-only mixed MR/material-plan PO smoke, G5/G6/G7 smokes, and a two-buyer 60+60 race against 100 demand passed. One buyer committed 60; the other was rejected; no over-allocation committed. That preview was deleted.
- Task 12 Cloud preview: guarded collaboration/lineage migration and rollback smoke passed persisted actor/time, keyset pagination, denied history, protected-reference payload, bidirectional lineage, and exact historical title/lines after the current plan title changed. That preview was deleted.

## Full-chain Cloud replay

- A disposable **schema-only** branch `project-v2-task13-20260924` was created from the linked Supabase Cloud project. Automatic migration replay reported `MIGRATIONS_FAILED`. Manual tracked replay reached the older `20260910031856_authorization_v2_phase4_remaining_enforced_rooms.sql` and failed because its required Room-binding rows were absent from the schema-only clone. The branch was deleted. This is a concrete data prerequisite in a pre-V2 migration, not a Project V2 SQL assertion.
- A disposable **with-data** Cloud branch `project-v2-task13-data-20260924` had 113 historical migrations through `20260923042822` and 72 Room bindings. Its pooled Postgres URL rejected authentication, so the reviewed SQL was submitted only to the branch through the [official Management API query endpoint](https://supabase.com/docs/reference/api/v1-run-a-query), using `.env` credentials. The exact branch reference was checked before every request; no production SQL was submitted.
- The first five tracked files after the cloned head applied. Full tracked replay then **stopped** at the unrelated committed `20260923091500_daily_log_publish_progress_permission.sql`: existing `project_permission_rooms` rows violated `project_permission_rooms_allowed_actions_check`. The remaining daily-log files were not applied. The five remaining Project V2/Procurement V2 files applied successfully, completing the V2 schema chain on that preview. The query endpoint does not update `supabase_migrations.schema_migrations`; preview migration-history parity therefore remained at 113 and is **not** claimed as passing.
- Rollback-only Cloud smokes passed for planning, Task 6 availability/crews, Task 7 material candidates, Task 8 material-plan intake, composed Task 8 + Task 9 dossier read, and Task 12 collaboration/lineage. A post-smoke read found zero synthetic fixture projects. The mixed-source PO smoke stopped at `ERP_COMPLETION_PILOT_COMMAND_DISABLED`: the copied G9 command gate correctly rejected the synthetic project, which had no G9 rollout scope. Task 11's earlier isolated Cloud smoke and two-buyer race remain the PO evidence; the data-backed preview does not add a full-chain PO pass.
- Read-only schema checks on the data-backed preview: RLS enabled on all nine `public.project_v2_*` tables; 34 V2-named functions, 33 security definers, zero definers missing a configured `search_path`, and zero anonymous execution grants. Security Advisor returned 10 V2 `INFO` no-policy findings (tables are intentionally reached through guarded RPCs) and 20 `WARN` findings for authenticated security-definer functions; these need release review against the server guards. Performance Advisor returned 19 V2 `INFO` unindexed-FK findings and no V2 warning/error. The preview was deleted.

## Outstanding release gates

### RICO pilot target — 25 September read-only Cloud audit

The business owner named **DỰ ÁN RICO** (`PRJ-12788C72`) for the V2 pilot: Nguyễn Phương Thảo plans, Dương Xuân Thịnh independently approves the month/construction/material plans, Nguyễn Văn Năm creates POs, and Nguyễn Thị Mơ approves POs and handles warehouse/finance. Production Cloud identifies one exact project/site pair: project `12788c72-391c-4fcf-8d8c-71112eeab3d4` and `Công trường RICO` with the same site ID. This is distinct from the two other RiCo-named projects (`DA25001`, `DA25001LT`), which must not be enrolled by a name search. All four named accounts are active. Thảo and Mơ are already project staff scoped to that site; Thịnh and Năm are **not** yet RICO project staff and have no RICO Room bindings. Thảo currently has no project Room membership; Mơ has legacy Rooms including `material_po` with `view/edit/submit` but **no `approve`**, and none has V2 Room grants (the V2 catalog is not deployed). No V2 workspace was created.

RICO has 74 work BOQ items, one customer contract item, eight manual material BOQ lines with quantity and unit, and one site warehouse (`Kho RICO`). Six BOQ lines resolve to existing inventory items; two reference missing item records and must remain unresolved. It has **zero active work BOQ norm mappings, zero selected material component estimates, and zero contract material resources**. The V2 material-plan candidate reader cannot produce a complete material derivation until an authoritative, versioned norm/UOM mapping is supplied for at least one chosen work item. No norm, quantity, permission or staff assignment was invented or backfilled. Thịnh and Năm need RICO site staff enrollment through the existing guarded flow before scoped V2 Room grants; Mơ needs an explicit `material_po.approve` grant if she is to approve POs.

The owner chose `Đổ bê tông lót → Bê tông M300` and `Đào móng → Cát trát` as the first pilot pairs, and clarified that 100 is the **total material quantity**, not a per-work-unit consumption norm. Keep Cát trát in `m³`; the earlier `100 tấn` answer was superseded by this clarification. Read-only Cloud verification found both existing manual BOQ lines at **100 m³**, linked respectively to work BOQ lines `Đổ bê tông lót` (8.91406 m³ of work) and `Đào móng` (420.04425 m³ of work). The BOQ totals cannot be used as approved consumption coefficients or a source norm; neither mapping has been entered. `npm run check:supabase-migrations` was rerun on 25 September and failed on 14 unallowlisted post-baseline migration files, including V2 and unrelated daily-log/material-request work. The dirty shared marker must be reconciled in a scoped release change.

### RICO access and app verification — 25 September

After the owner said the additional permissions were granted, production Cloud read-only checks found all four named users as active RICO site staff. Mơ now has active `material_po.approve`. Thảo, Thịnh and Năm also have active `material_po.approve`; these broad legacy Room grants exceed their named planner/plan-approver/buyer duties and need review before persona UAT. Production still has 116 migrations through `20260924165000`, no `project_v2_workspaces` table, **zero** V2 Room catalog entries (`v2_month_plan`, `v2_construction_plan`, `v2_material_plan`), zero RICO V2 Room members, and zero active RICO work BOQ norm mappings. Thus the actual RICO V2 workflow cannot be exercised on production, regardless of legacy grants. No impersonated login or business signoff was claimed.

Technical test replay on the current checkout: 46 focused Vitest cases passed across six Project V2 candidate/BOQ/domain/command/read files; 11 Project V2 planning/collaboration and five Procurement V2 dossier Playwright fixture tests passed at desktop/tablet/mobile widths and zoom cases. These use sample UI data, not RICO Cloud commands. The migration baseline check remains red as above. No production data was written during this verification.

The owner then corrected the PO Room grants. A fresh read-only Cloud check confirms all four remain RICO site staff and **only Mơ has active `material_po.approve`** among the four. All four still have `material_po.submit`; Năm is the named PO creator, so planner/plan-approver PO submission access needs role-scope review before persona UAT. Both selected pilot BOQ materials resolve to existing inventory items. The production V2 schema/Rooms and RICO norm mappings remain absent. The two missing inventory references on other BOQ lines do not block identity for these first two pairs, but must remain visible as unresolved if those lines enter a plan. The owner needs to identify approved per-work-unit consumption coefficients/source norms for the two pairs; the 100 m³ totals alone cannot supply them. Technical migration/baseline reconciliation and V2-only grants can be prepared independently, with the concurrent untracked daily-log migration and dirty shared baseline marker left out of Project V2 commits.

The owner next specified **50 m³ of Bê tông M300 and 50 m³ of Cát trát as total quantities for the first pilot plans**, not consumption per unit of construction work. They remain proposed plan quantities distinct from the 100 m³ whole-project BOQ totals; no per-unit norm has been supplied or written. The V2 candidate reader and plan validation require a norm resource/revision, per-work-unit factor and construction-source quantity; an override cannot turn a missing norm into a publishable line. The owner also said they removed Thảo/Thịnh's PO-submit rights. A fresh read-only Cloud query after that message still found active `material_po.submit` rows for both (their `approve` rows are inactive). The latest `replace_project_permission_room_members` audit event at `2026-09-25 03:37:21+00` explicitly includes `submit` in both saved action lists. The Room drawer labels this action “Gửi”; the exact saved payload, not a delayed read, retained it. Its database state must be reconciled before a negative persona test. Do not claim the submit grant is gone based on the user's UI action alone. The concurrent daily-log migration remains untracked and out of scope for this Project V2 workstream.

### RICO tracked migration replay — 25 September

A second disposable **with-data** Cloud branch `project-v2-rico-parity-20260925` (`kzibfuthzaztzaweabdx`) was verified against the production parent/name/ref before writes. Its database password was changed on the preview only, allowing the repository's Supabase CLI to use the Cloud session pooler; the transaction pooler had rejected prepared statements. A CLI `db push --include-all --dry-run` listed the exact 16 missing files in repository order. The CLI applied them on that preview, including the corrected daily-log Room migration and the currently untracked `20260923103000_daily_log_summary_revisions.sql`. Management API history then showed 132 versions, all 16 expected filename versions and names matched, and a second CLI dry run reported **“Remote database is up to date.”** This closes the earlier API-generated timestamp mismatch **on the preview only**. The untracked daily-log file and dirty shared migration marker still prevent a clean repository release gate.

On this tracked-replay preview, rollback-only planning, source availability/crews, material candidate, project BOQ balance, collaboration/lineage, composed material-plan intake + dossier, and G9-scoped mixed MR/material-plan PO smokes all passed. Post-smoke checks found zero V2 workspaces, zero synthetic projects, the three V2 Room catalog rows, and zero RICO V2 members. Security Advisor again reported 10 V2 INFO no-policy and 20 V2 WARN authenticated-definer findings; Performance Advisor reported 19 V2 INFO unindexed-FK findings. The authenticated RPC warnings require release review alongside their server-side actor/scope guards; neither an automated smoke nor advisor output is business signoff. The disposable branch was deleted after its name, parent, project ref and data-clone flag were verified; it is absent from the parent's branch inventory, and its temporary credentials were removed. No production V2 migration, cohort, staff assignment, Room grant or norm mapping was changed.

### 25 September data-backed Cloud gate replay

Production read-only checks found 116 recorded migrations through `20260924165000`, no `project_v2_workspaces` table, and no Project V2 cohort. A disposable **with-data** Cloud branch `project-v2-real-data-gate-20260925` (`tllaaibuvnepvlqpljig`) was created from the verified production ref. Before each write, its name, parent ref, branch ref, data-clone flag and healthy status were checked. No Project V2 SQL was submitted to production.

The committed daily-log publication migration was shown by a failing regression test to drop Project V2's `return` action from both Room constraints. Adding `return` to those allowlists made the test pass. The corrected file and the other missing schema files then applied in version order to the data-backed preview, including the otherwise untracked `20260923103000_daily_log_summary_revisions.sql`. The Management API migration endpoint assigned fresh `20260925...` versions rather than the repository filename versions. Thus **the schema replay passed but tracked migration-version parity did not**; neither the untracked daily-log revision nor the dirty shared migration marker is being included in the Project V2 commit. A rapid pair of API submissions briefly collided on a generated timestamp; retry after the next second succeeded with no SQL defect.

Rollback-only preview smokes passed for planning, Task 6 source availability/crews, Task 7 material candidates, project BOQ position, Task 12 collaboration/lineage, and composed Task 8 intake + Task 9 dossier. The mixed MR/material-plan PO smoke passed after adding a transaction-local G9 rollout scope for its synthetic project and making its actor-spoof probe name that same scope; the ordinary unscopeable `{}` probe is rejected by the G9 gate before actor validation. All fixture changes rolled back. A read-only post-check found zero synthetic fixture projects and zero V2 workspaces. Nine public V2 tables had RLS enabled; among 37 V2-named functions, no security definer lacked `search_path` and none was executable by `anon`. Security Advisor returned 10 V2 INFO no-policy findings and 20 V2 WARN authenticated-definer findings; Performance Advisor returned 19 V2 INFO unindexed-FK findings. These remain for release review. Production still had no V2 workspace table after the replay.

The disposable preview was deleted and its absence confirmed in the branch inventory. This is a technical data-clone replay, **not** an actual app-route/persona journey or business signoff. Production deployment remains closed until repository/Cloud migration-version parity, shared baseline resolution, named cohort/persona preparation, advisor review and business UAT are complete.

On 25 September, the Project V2 planning shell and sample were refreshed using the visual cues of the existing “Hôm nay” page: a compact dark project header, white planning cards, restrained indigo/cyan/green step icons, and clearer form hierarchy. The sample now says its data is illustrative and removes the nonfunctional submit action. The real Project V2 workspace uses the same shell and matching plan tabs. Playwright covered the sample at 390/768/1440 px and 200% zoom; all seven checks passed. This is visual/interaction evidence only and does not substitute for an actual route or business-persona test.

The 24 September material-plan follow-up connected the prepared project-wide BOQ reader to the material editor and plan detail. Focused BOQ/UI tests and the planning fixture walkthrough at 390/768/1440 px (6) passed; the Vite build and scoped TypeScript check passed. The repository-wide TypeScript check remains red only under the unrelated untracked `docs/references` prototype. The input renders over-receipt as a negative remaining quantity without crashing, and unresolved/pending figures stay explicitly unknown.

A disposable **with-data** Cloud branch `project-v2-boq-reader-20260924` (`gntvedmuzdxnhswysgoi`) cloned production with 116 migration-history rows through `20260924165000`. The ten Project V2/Procurement V2 migration files, including `20260924081548_project_v2_material_boq_position.sql`, were applied to that preview through the Management API after checking the exact branch parent/name/ref. The new public RPC is a security-invoker wrapper; the privileged reader is in `app_private`, and anonymous execution is denied. A rollback-only smoke passed for known zero receipts, confirmed site receipts, supplier returns, mixed BOQ source/unit uncertainty, outside-BOQ material and out-of-scope actor denial. Post-smoke workspace count was zero. Security and performance advisors reported no finding naming either new reader function. The preview was deleted, and its absence was confirmed in the branch inventory. Manual SQL did **not** advance migration history, so full tracked migration parity and the separate daily-log chain remain open. No production V2 migration or cohort was enabled. The pending supply field remains unknown until its own authoritative reader exists.

1. Resolve the separate daily-log Room constraint failure in its owning workstream, reconcile the shared migration marker, and rerun a full tracked Cloud migration replay with migration-history parity. Review the advisor warnings and run a G9-scoped mixed-source PO smoke. Do not apply Project V2 migrations to production until these gates are reviewed.
2. Run actual app-route journeys with a named sample project/site and separate planner, approver, buyer, warehouse and finance personas. Component fixtures and synthetic SQL tests do not provide this evidence.
3. Complete the [Project V2 pilot runbook](../../../runbooks/project-v2-pilot-rollout.md) UAT records with actual results, cleanup and named business signoff. Automation must not be marked as business approval.
4. The repository-wide lint and migration-baseline gates remain red while unrelated untracked/dirty workstreams are unresolved. Do not mix those files into a Project V2 commit.
